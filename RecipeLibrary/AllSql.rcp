#auto
#version 1.5+

#tags sql database i360 all

Get all the latest *All.sql files in one file to execute
in SQL Server Manager

#put output=c:\temp\all.sql

#load all=-r C:\TeamImarda\Imarda360\Imarda360.DatabaseScripts\Imarda360.*All.sql

#newer `output`
`all`
#end

#run {`output`}