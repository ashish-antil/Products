#region

using System;
using Cormit.Business.RouteTracking.Constants;

#endregion

namespace Cormit.Business.RouteTracking.Helpers
{
	public static class AttributesHelper
	{
        //public static void UpdateAttributes(Unit unit, EntityAttribute attribute, object value)
        //{
        //    unit.UpdateAttributes(Enum.GetName(typeof(EntityAttribute), attribute) + "|" + value);
        //}
	}
}